{
  "name": "mainichi-tarots",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "description": "毎日タロット＆占い — GitHub Pages 占い SPA",
  "scripts": {
    "dev": "vite",
    "build": "tsc -b && vite build",
    "preview": "vite preview",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "preact": "^10.22.0",
    "preact-iso": "^2.7.2"
  },
  "devDependencies": {
    "@preact/preset-vite": "^2.9.0",
    "typescript": "^5.5.4",
    "vite": "^5.4.0"
  }
}
